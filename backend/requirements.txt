fastapi[standard]>=0.104.0
uvicorn[standard]>=0.24.0
sqlalchemy[asyncio]>=2.0.23
asyncpg>=0.29.0
alembic>=1.13.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-jose[cryptography]>=3.3.0
# passlib 1.7.x reads `bcrypt.__about__.__version__`, which was removed in
# bcrypt 5.0. With bcrypt 5.x, passlib's backend probe fails and every
# hash/verify call raises ValueError regardless of input length. Pin to
# 4.x (still ships __about__) until passlib 2.x ships (upstream effectively
# unmaintained).
passlib[bcrypt]>=1.7.4
bcrypt>=4.1,<5.0
python-multipart>=0.0.6
httpx>=0.27.0
fastembed>=0.4.0
pgvector>=0.2.4
redis>=5.0.0
structlog>=24.1.0
cachetools>=5.3.0
# Direct runtime deps (were satisfied transitively — pin explicitly so an
# upstream dropping them silently doesn't break the app):
#   python-dotenv: backend/alembic/env.py loads .env for the Alembic CLI
#   PyYAML:        backend/app/services/skill_loader.py parses frontmatter
#   PyJWT[crypto]: backend/app/services/github_app_jwt.py signs RS256 JWTs
#                  for GitHub App auth — needs cryptography for RS256.
#   networkx:      backend/app/services/code_indexer/* graph operations.
#   graphifyy:     code-graph indexer (Leiden clustering). pip name has
#                  TWO y's; the Python module is `graphify` (single y).
#   tzdata:        guarantees IANA legacy aliases (Asia/Calcutta, US/Eastern,
#                  …) resolve via zoneinfo on Debian-slim images, which ship
#                  only the canonical zones by default.
python-dotenv>=1.2.2
PyYAML>=6.0
PyJWT[crypto]>=2.8.0
networkx>=3.2
graphifyy[leiden]>=0.5.5,<0.6
tzdata>=2026.2
